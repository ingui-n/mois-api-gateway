# MOIS API Gateway

## Setup

Move `.env-template` to `.env` and fulfill the environments.

## For debugging

Install dependencies:

```bash
bun install
```

Run:

```bash
bun run index.js
```

## For deployment

```bash
docker-compose up -d
```
